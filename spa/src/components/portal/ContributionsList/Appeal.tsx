import PropTypes, { InferProps } from 'prop-types';

const AppealPropTypes = {};

export function Appeal() {
  return <p>Appeal goes here</p>;
}

Appeal.propTypes = AppealPropTypes;
export default Appeal;
