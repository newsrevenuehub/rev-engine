import styled from 'styled-components';
import BaseProviderInfo from 'components/connect/BaseProviderInfo';

export const StripeProvider = styled(BaseProviderInfo)``;

export const StripeRestricted = styled.p``;

export const StripeConnected = styled.p``;
