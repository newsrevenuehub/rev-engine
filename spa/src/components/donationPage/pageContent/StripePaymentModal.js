import PropTypes from 'prop-types';

import Modal from 'elements/modal/Modal';

function StripePaymentModal({ stripeClientSecret }) {
  return <>Stripe payment modal!</>;
}

export default StripePaymentModal;
