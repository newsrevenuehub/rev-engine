import styled from 'styled-components';

export const BenefitsWidget = styled.div`
  padding: 2rem 0;
  border-bottom: 1px solid;
  border-color: ${(props) => props.theme.colors.grey[0]};
`;

export const SelectWrapper = styled.div``;
