import ContributorTokenExpiredModal from './ContributorTokenExpiredModal';

const meta = {
  title: 'Contributor/ContributorTokenExpiredModal',
  component: ContributorTokenExpiredModal,
};

export default meta;

export const Default = {};
Default.args = {
  isOpen: true
};
